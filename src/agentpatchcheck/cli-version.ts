/**
 * Public version of the independently distributed Headless CLI. Keep this in
 * lockstep with package.json; the contract test makes a release mismatch fail.
 */
export const HEADLESS_CLI_VERSION = "0.1.70";
