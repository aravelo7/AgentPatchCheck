Update the managed repository in exactly one apply-edit-batch tool call. Change src/edition/status.ts so it exports editionStatus as "published", and create src/edition/summary.ts with exactly these two statements: `import { editionStatus } from "./status";` and `export const editionSummary = `${editionStatus}: managed`;`. Do not modify any other file. After the batch succeeds, run the Harness-owned run-public-verification tool with index 0, then call finish.
