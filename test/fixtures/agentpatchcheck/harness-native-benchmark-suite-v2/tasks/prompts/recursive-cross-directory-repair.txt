Use only the managed workspace tools. First use `search-text-recursive` from `.` to locate the nested source files containing `cross-before-a` and `cross-before-b`. Replace those exact values with `cross-after-a` and `cross-after-b` respectively. Preserve all other content, do not modify any other file, and finish after both replacements succeed. Do not assume any hidden checks exist.
