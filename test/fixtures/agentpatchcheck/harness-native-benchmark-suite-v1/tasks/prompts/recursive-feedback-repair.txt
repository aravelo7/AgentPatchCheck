Use only the managed workspace tools. On this initial attempt, use `search-text-recursive` from `.` to locate the nested source file containing `cross-before-a`. Replace that exact value with `cross-after-a`, do not modify the file containing `cross-before-b`, and finish. If the Harness later supplies public verification feedback, inspect `src/config/beta.ts`, replace its exact `cross-before-b` value with `cross-after-b`, then finish without further discovery. Preserve all other content and do not assume any hidden checks exist.
