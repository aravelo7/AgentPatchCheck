Use only the managed workspace tools. Update the existing files `first.txt` and `second.txt`: replace `first-before` with `first-after` in the first file, and replace `second-before` with `second-after` in the second file. Preserve the trailing newlines and do not modify any other file. After both replacements succeed, call finish without extra inspection tools. Do not assume any hidden checks exist.
