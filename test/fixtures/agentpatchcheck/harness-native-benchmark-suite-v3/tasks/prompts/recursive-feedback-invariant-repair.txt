Use only the managed workspace tools. On this initial attempt, use `search-text-recursive` from `.` to locate the nested source file containing `cross-before-a`. Replace that exact value with `cross-after-a`, do not modify the file containing `cross-before-b` or any other file, and finish. If the Harness later supplies public verification feedback, inspect `src/config/beta.ts`, replace its exact `cross-before-b` value with `cross-after-b`, and finish. Preserve all unrelated files exactly; do not assume any hidden checks exist.
